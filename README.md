Esoteric language inspired by LOLCODE. Thought it'd be something silly to work on while I learn about compilers. 


Set build target:
```bash
cmake -S . -B build/
```

Build on your own:
```bash
cmake --build build/
```

Run program:
```bash
./out
```